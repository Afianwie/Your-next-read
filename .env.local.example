# Copy this file to .env.local and fill in the secret key.
# .env.local is gitignored — it must never be committed or pasted into a chat.
#
# Find the secret key at:
#   Supabase dashboard -> Project Settings -> API Keys -> secret key
#
# This key bypasses Row Level Security entirely. It belongs only in this
# file and on trusted machines. It must never appear in any file the
# browser loads (js/supabase-config.js uses the publishable key instead).

SUPABASE_URL=https://oprumtyxnwyddwnqxozx.supabase.co
SUPABASE_SECRET_KEY=paste_your_secret_key_here
